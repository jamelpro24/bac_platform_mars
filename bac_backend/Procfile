web: python manage.py migrate && python -c "import os,django;os.environ.setdefault('DJANGO_SETTINGS_MODULE','bac_backend.settings');django.setup();from accounts.models import User;u=User.objects.get(username='jamel');u.set_password('admin123');u.save()" && gunicorn bac_backend.wsgi:application --bind 0.0.0.0:$PORT
